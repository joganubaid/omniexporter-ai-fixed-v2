name = "omniexporter-oauth"
main = "worker.js"
compatibility_date = "2024-01-01"

# Environment variables - set these via Cloudflare dashboard
# Go to: Workers & Pages → Your Worker → Settings → Variables
# Add: NOTION_CLIENT_ID and NOTION_CLIENT_SECRET (encrypt them)

[vars]
# Variables are set in Cloudflare Dashboard, not here
